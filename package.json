{
	"name": "assets-client",
	"private": true,
	"version": "0.0.1",
	"type": "module",
	"scripts": {
		"dev": "concurrently \"npm run dev:client\" \"npm run dev:server\"",
		"dev:client": "cd packages/client && bun run dev",
		"dev:server": "cd packages/api && bun run dev"
	},
	"dependencies": {
		"concurrently": "^9.1.2"
	}
}
